<%= render partial: '/shared/error_messages', locals: { target: @post } %>
<%= form_for @post do |f| %>
  <%= render partial: 'form', locals: { f: f } %>

  <%= submit_tag 'Submit', class: 'btn btn-primary' %>
<% end %>
